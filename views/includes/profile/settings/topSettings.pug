h3 Профиль
div(class="block")
  div(class="top-settings-box")
    div(class="avatar-box")
      div
        img(src='/uploads/'+currentAccount.avatar alt=currentAccount.username)
      div(class="change-avatar-box") 
        button(class="btn btn-success change-avatar-btn") Обновить аватар
        form(enctype="multipart/form-data" method="post" class="file-upload-form" class="hide")
          input(type='file' name="avatar")

    div(class="fields-box")
      div(class="form-row2 mb-3 nik-box")
        label(class="form-label") Ник*
        div(class="accept-input-box")
          input(class="form-control" maxlength=30 value=currentAccount.username data-val=currentAccount.username)
          button(class="btn btn-success bi-check accept" title=`Стоимость смены ника: ${nikChanges.length*100} рублей`)

      div(class="form-row2 mb-3")
        label(class="form-label") Почта
        input(class="form-control" disabled value=currentAccount.email)

      div(class="mb-3")
        label(class="form-label") Пол
        select(class="form-control" id="gender")
          option(value="0") Не указан
          option(value="1" selected=currentAccount.gender==1) Мужской
          option(value="2" selected=currentAccount.gender==2) Женский

      div(class="mb-3")
        label(class="form-label") Инвентарь
        select(class="form-control" id="inventhide")
          option(value="0") Виден всем
          option(value="1" selected=hideinvent==1) Виден только друзьям
          option(value="2" selected=hideinvent==2) Скрыт для всех

      div(class="mb-3")
        label(class="form-label") Звуковое уведомление о начале партии
        select(class="form-control" id="gameplaysound")
          option(value="0") Включено
          option(value="1" selected=gameplaysound==1) Выключено

      if currentAccount.telegramChatId
        div(class="mb-3")
          label(class="form-label") Уведомление в Telegram о начале партии
          select(class="form-control" id="gamenotify")
            option(value="1" selected=gamenotify==1) Уведомлять всегда
            option(value="2" selected=gamenotify==2) Уведомлять только если я оффлайн

      div(class="mb-3")
        label(class="form-label") Тема
        select(class="form-control" id="skin")
          option(value="0" selected=currentAccount.skin==0) Светлая
          option(value="1" selected=currentAccount.skin==1) Тёмная
          option(value="2" selected=currentAccount.skin==2) Зелёная
          option(value="3" selected=currentAccount.skin==3) Нефритовая
          option(value="4" selected=currentAccount.skin==4) Синяя

      if currentAccount.vip
        div(class="change-bg-box")
          div
            label(class="form-label") Фон страницы
          div(class="bg-image-box mb-2")
            if currentAccount.bg  
              img(src=`/uploads/${currentAccount.bg}` width="100%")
            else 
              small Не установлен
          button(class="btn btn-success change-bg-btn") Сменить фон
          if currentAccount.bg
            button(class="btn btn-danger remove-bg-btn ms-2") Удалить фон

          form(enctype="multipart/form-data" method="post" action="/changebg" class="hide")
            input(type='file' name="bg")

