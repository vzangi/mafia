mixin switch(cls)
  div
    label(class="switch")
      input(type="checkbox" class=cls name=cls id=cls)
      span(class="slider round")

mixin settings_item(name, cls)
  div(class="setting-item")
    div(class="me-2")
      span!= name 
      if block 
        block
    +switch(cls)